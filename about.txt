1.针对xlua修改框架，调试socket

2.解决动画问题(使用animator，并添加状态机)
3.调试打包程序（包括：lua加解密、prefab、动作等）
3.制作临时登陆界面
4.制作临时大厅
5.制作临时战场
6.不同级别的子弹特效
7.鱼的被击和死亡，击杀帐号奖励



mac os 更新 git
避免git可用存储空间不足
git config --global http.postBuffer 1048576000
避免带宽过低导致传输中断
git config --global http.lowSpeedLimit 0
git config --global http.lowSpeedTime 999999

关于HomeBrew
/usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)" 	//删除HomeBrew
/usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/uninstall)"	//安装HomeBrew
更新git
brew install git
重新指向新安装的git
brew link git --overwrite

unity git
初始设置
git config --global core.autocrlf false  //禁用自动转换 
git init
git remote add origin https://github.com/sacu/CatchFish.git

git pull https://github.com/sacu/CatchFish.git	//获取地址数据

每次提交设置
git add 文件或目录
git commit -m "注释"
git push -u origin master	//提交文件

强制获取远程覆盖本地
git fetch --all
git reset --hard origin/master 
git pull

###